schema_version: 1
required: [schema_version, event_id, run_id, run_type, type, ts, seq, data]
properties:
  run_type:
    enum: [session, turn, task]
  seq:
    type: integer
  data:
    type: object

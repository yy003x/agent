# 启动记忆

## 项目状态

构建阶段：P0–P4 骨架已搭建完成（首次初始化待执行）。
- 待执行：`python skills/content-generate/scripts/content_runtime.py init`（建库）
- 待执行：`bash scripts/validate.sh`（启动自检）
- 知识库为空，首次使用前需 `kb ingest` 导入素材。

## 关键偏好

- 内容领域：教育类图书（书单 / 读书笔记 / 知识卡片 / 读后感 / 书评）。
- 交付平台：小红书（默认）/ 朋友圈；成品包落 `outputs/`，**人工预览后手动发布**。
- 全本地运行：无外部生成 API，不自动发帖，不生成图片/视频（仅检索 KB 既有素材组装）。

## 上次更新

2026-06-18
